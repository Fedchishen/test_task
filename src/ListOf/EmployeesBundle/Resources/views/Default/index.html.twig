<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}">
            <link rel="stylesheet" href="{{ asset('css/treeline.css') }}">
            <script src="{{ asset('js/jquery.min.js') }}"></script>
            <script src="{{ asset('js/bootstrap.js') }}"></script>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}
            
        {# Рекурсивно выводим всех работников в древовидной форме#}
        {% include "@ListOfEmployees/Default/recurse.html.twig" 
           with {'employees': employees, 'position':position} only %}   
                
           
            {#{% for employe in employees %}
                {% if employe.id == 1 %}
                    {{ employe.firstname }}
                {% endif %}
            {% endfor %}
            <table>
            {% for employe in employees %}
            <tr>
                    <td>{{ employe.id }} </td>
                    <td>{{ employe.firstname }} </td>
                    <td>{{ employe.middlename }} </td>
                    <td>{{ employe.surname }} </td>
                    <td>{{ employe.positionid }} </td>
                    <td>{{ employe.dateofemployment|date('Y-m-d') }} </td>
                    <td>{{ employe.wages }} руб </td>
                    <td> {{ employe.chiefid }} </td>
            </tr>
            {% endfor %}
            </table>#}

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>

